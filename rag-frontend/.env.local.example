# URL of your FastAPI backend. For local dev this is fine as-is.
# For a Vercel deployment, set this in Vercel's project settings to your
# deployed backend URL (e.g. https://your-app.onrender.com) -- NOT localhost.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
